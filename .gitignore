# There is no build step and no package manager here, so there is very little
# to ignore. Keep it that way: if something in this list starts appearing, it
# probably means a tool has been introduced that the site does not need.

# Editor and OS clutter
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Local experiments
/local/
*.log
